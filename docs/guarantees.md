# Guarantees & Non-Goals

**Guarantees**
- Outputs a web-ready kit (fonts, CSS, tokens, specimen).
- Deterministic output given the same tool version, inputs, and settings (metadata may vary).

**Non-Goals**
- Not a full font editor.
- Does not generate custom glyphs.
