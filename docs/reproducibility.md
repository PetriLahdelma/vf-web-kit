# Reproducibility

The tool aims for deterministic outputs with the same inputs and settings.
Some font toolchains embed timestamps or non-deterministic metadata, which can prevent byte-identical output.
