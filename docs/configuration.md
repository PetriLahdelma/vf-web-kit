# Configuration

`vfkit.config.json` is auto-detected if present, or pass `--config <path>`.

```json
{
  "content": "./content/**/*.{html,md,mdx,tsx}",
  "out": "./dist/fontkit",
  "preset": "ui"
}
```
