# Constraints & Limitations

- Requires explicit content or strings to extract glyphs.
- Presets are limited to `ui` and `editorial`.
- Some fonts may not support requested axis ranges.

Performance notes: scales with glyph count and font size; subsetting dominates.
