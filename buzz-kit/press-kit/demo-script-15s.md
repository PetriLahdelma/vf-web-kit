**15s Demo Script**
- "Variable fonts are hard to ship. This makes it one command."
- Run `npx vf-web-kit path/to/font.ttf --content "./content/**/*.{html,md,mdx,tsx}" --out ./dist/fontkit`.
- "You get a subset, CSS, tokens, and a QA specimen. Done."
